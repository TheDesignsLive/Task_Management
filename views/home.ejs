<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Home</title>
  <style>
    body {
      margin: 0;
      font-family: Arial, sans-serif;
      background-color: #3C3A3A; /* main background */
    }

    /* Layout below navbar */
    .layout {
      display: flex;
      min-height: calc(100vh - 60px); /* adjust if navbar height changes */
    }

    /* Sidebar */
    .sidebar-container {
      width: 55px; /* match your sidebar width */
    }

    /* Main content next to sidebar */
    .main-content {
      flex: 1;
      display: flex;
      flex-direction: column;
      padding: 0;
      margin-left: 0;
    }

    /* Sub-header */
    .sub-header {
      display: flex;
      background-color: #CDF4F4;
      padding: 10px;
      gap: 10px;
      align-items: center;
    }

    .sub-header .task {
      flex: 4; /* biggest */
      padding: 8px;
      text-align: center;
      border-radius: 5px;
      cursor: pointer;
      background-color: #CDF4F4;
    }

    .sub-header .change,
    .sub-header .update {
      flex: 1.5; /* medium */
      padding: 8px;
      text-align: center;
      border-radius: 5px;
      cursor: pointer;
      background-color: #CDF4F4;
    }

    .sub-header .other-task {
      flex: 2; /* smallest */
      padding: 8px;
      text-align: center;
      border-radius: 5px;
      cursor: pointer;
      background-color: #CDF4F4;
      display: flex;
      justify-content: center;
      align-items: center;
    }

  </style>
</head>
<body>

  <!-- Navbar at top -->
  <%- include('navbar') %>

  <!-- Layout below navbar -->
  <div class="layout">
    <!-- Sidebar on the left -->
    <div class="sidebar-container">
      <%- include('sidebar') %>
    </div>

    <!-- Main content on the right -->
    <div class="main-content">
      <!-- Sub-header -->
      <div class="sub-header">
        <div class="task">Task</div>
        <div class="change">Change</div>
        <div class="update">Update</div>
        <div class="other-task">Other Task</div>
      </div>

      <!-- Rest of content goes here -->
    </div>
  </div>

</body>
</html>
