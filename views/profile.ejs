<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Profile</title>

<style>
body{
  margin:0;
  font-family: 'Segoe UI', Tahoma, sans-serif;
  background:#3C3A3A;
}
.layout{
  display:flex;
  min-height:calc(100vh - 60px);
}
.sidebar-container{
  width:55px;
}
.main-content{
  flex:1;
  padding:25px;
  background:#f4f6f9;
  display:flex;
  justify-content:center;
  align-items:flex-start;
}
.profile-card{
  width:420px;
  background:#fff;
  border-radius:12px;
  box-shadow:0 10px 30px rgba(0,0,0,0.15);
  overflow:hidden;
  text-align:center;
}
.profile-header{
  background:linear-gradient(135deg,#095959,#00a896);
  padding:35px 20px 60px;
  color:white;
  position:relative;
}
.profile-img{
  width:110px;
  height:110px;
  border-radius:50%;
  border:4px solid white;
  object-fit:cover;
  position:absolute;
  left:50%;
  transform:translateX(-50%);
  bottom:-55px;
  background:#ddd;
}
.profile-body{
  padding:70px 25px 25px;
}
.profile-name{
  font-size:20px;
  font-weight:600;
  color:#333;
}
.profile-role{
  font-size:13px;
  color:#888;
  margin-bottom:20px;
}
.info-box{
  background:#f7f7f7;
  padding:12px;
  border-radius:6px;
  margin-bottom:12px;
  text-align:left;
  font-size:14px;
}
.info-box strong{
  color:#095959;
}
.profile-btn{
  margin-top:15px;
  width:100%;
  padding:10px;
  border:none;
  border-radius:6px;
  background:linear-gradient(135deg,#095959,#00a896);
  color:white;
  font-size:14px;
  cursor:pointer;
  transition:0.3s;
}
.profile-btn:hover{
  opacity:0.9;
  transform:translateY(-1px);
}
</style>
</head>

<body>

<%- include('navbar') %>

<div class="layout">

  <div class="sidebar-container">
    <% if (show_sidebar === 'sidebar') { %>
        <%- include('sidebar') %>
    <% } else { %>
        <%- include('user_sidebar') %>
    <% } %>
  </div>

  <div class="main-content">

    <div class="profile-card">

      <div class="profile-header">
        <h3>My Profile</h3>
        <img src="<%= profilePic ? '/uploads/' + profilePic : '/default-profile.png' %>" class="profile-img">
      </div>

      <div class="profile-body">

        <div class="profile-name"><%= name %></div>
        <div class="profile-role"><%= role %></div>

        <div class="info-box"><strong>Email:</strong> <%= email %></div>
        <div class="info-box"><strong>Company:</strong> <%= company || 'N/A' %></div>
        <div class="info-box"><strong>Account Type:</strong> <%= role %></div>
        <div class="info-box"><strong>Status:</strong> Active</div>

        <button class="profile-btn" onclick="openEdit()">Edit Profile</button>
      </div>

    </div>

  </div>

</div>

<div class="modal" id="editModal" style="display:none;position:fixed;inset:0;background:rgba(0,0,0,0.6);justify-content:center;align-items:center;">
<div style="width:420px;background:white;border-radius:10px;padding:20px;">

<h3>Edit Profile</h3>

<form action="/profile/update-profile" method="POST" enctype="multipart/form-data">

<input type="text" name="name" value="<%= name %>" placeholder="Name" required style="width:100%;padding:8px;margin-bottom:10px;">
<input type="email" name="email" value="<%= email %>" placeholder="Email" required style="width:100%;padding:8px;margin-bottom:10px;">
<input type="text" name="phone" value="<%= phone %>" placeholder="Phone" style="width:100%;padding:8px;margin-bottom:10px;">
<% if(r=="admin"){ %>
<input type="text" name="company" value="<%= company %>" placeholder="Company" style="width:100%;padding:8px;margin-bottom:10px;">
<% } %>
<input type="file" name="profile_pic" style="margin-bottom:10px;">

<div style="display:flex;gap:10px;">
<button style="flex:1;background:#00a896;color:white;border:none;padding:10px;">Save</button>
<button type="button" onclick="closeEdit()" style="flex:1;background:#ddd;border:none;padding:10px;">Cancel</button>
</div>

</form>
</div>
</div>

<script>
function openEdit(){document.getElementById('editModal').style.display='flex';}
function closeEdit(){document.getElementById('editModal').style.display='none';}
</script>

</body>
</html>
